var arr = [
    {
        userName:"Test",
        lastName:"Test",
        email:"test.test@gmail.com"
    },
    {
        userName:"Dmitro",
        lastName:"Porohov",
        email:"dmitro.porohov@yahoo.com"
    },
    {
        userName:"Andrii",
        lastName:"",
        email:"andrii@mail.ru" // Нам такі не підходять
    },
];

let newArr = [];
for (let i = 0; i < arr.length; i++) {
    let re = /\w+?\.?\w+@/;
    console.log(re.test(arr[i].email)); 
    const pattern = /mail.ru/;
    if (pattern.test(arr[i].email) === false) {
    newArr.push(arr[i]);
    }
}
console.log(newArr);
